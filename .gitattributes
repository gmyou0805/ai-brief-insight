# 이 저장소는 Windows(core.autocrlf=true)에서 작업 중이라, 컨테이너 안에서
# 그대로 실행되는 파일들은 체크아웃 시 CRLF로 바뀌면 깨질 수 있음(특히 crontab).
# LF로 고정.
docker/crontab text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
*.sh text eol=lf
